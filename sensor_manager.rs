pub fn read_sensor_data() -> Vec<u8> {
    // Read sensor data here
    // For example, if you're using a temperature sensor, read the temperature value.
    // Return the data as a vector.
    vec![0; 10] // Sample data
}

pub fn process_sensor_data(data: Vec<u8>) {
    // Process sensor data (e.g., log it)
    // ...
}
